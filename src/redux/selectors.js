export const articles = (state) => state.articles.articles;
export const loading = (state) => state.articles.loading;
export const error = (state) => state.articles.error;
export const articlesCount = (state) => state.articles.articlesCount;
export const userObj = (state) => state.user;
export const userErrors = (state) => state.user.errors;
